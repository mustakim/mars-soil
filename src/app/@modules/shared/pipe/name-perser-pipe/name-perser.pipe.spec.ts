import { NamePerserPipe } from './name-perser.pipe';

describe('NamePerserPipe', () => {
  it('create an instance', () => {
    const pipe = new NamePerserPipe();
    expect(pipe).toBeTruthy();
  });
});
